通知服务的 TS 编译还是过不去，npm run build 跑下来 4 处一模一样的错：Property 'changes' does not exist on type 'Statement'，全部在 src/db/notification.db.ts、preference.db.ts、template.db.ts 里，定位到第 62/90/84/91 行附近，形式都是 `const result = stmt.run(...); return result.changes > 0;`。问题是项目用的 sql.js 而不是 better-sqlite3，sql.js 的 stmt.run() 返回的是 Statement 类型（看 src/types/sql.js.d.ts 第几行那个 run() 签名返回 Statement），上面根本没有 changes 字段；sql.js 要拿受影响行数应该调 `db.getRowsModified()` 拿到 number，inapp.db.ts 里就是用 getRowsModified() 写的没问题，但 notification/preference/template 这三个 db.ts 还是 better-sqlite3 风格。检查修复一下，让 npm run build 能过、tsc 不报 4 个 Property 'changes' 错误。
